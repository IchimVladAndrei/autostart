server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          # may be changed
          uri: "file:///${user.home}/Documents/cursuri/autostart/config-repo"
          clone-on-start: true
          default-label: master # may be changed

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/