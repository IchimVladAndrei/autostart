spring:
  application:
    name: backend
  profiles:
    active: dev
  datasource:
    url: ${DB_URL:jdbc:h2:mem:devdb}
    username: ${DB_USER:sa}
    password: ${DB_PASSWORD:}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

